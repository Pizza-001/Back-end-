<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pet.hospital.mapper.BizVaccineRecordMapper">
    <select id="findAll" resultType="com.pet.hospital.model.entity.BizVaccineRecord">
        SELECT * FROM biz_vaccine_record ORDER BY id DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO biz_vaccine_record(pet_id, vaccine_name, inject_time, next_inject_time, remarks, create_time)
        VALUES (#{petId}, #{vaccineName}, #{injectTime}, #{nextInjectTime}, #{remarks}, NOW())
    </insert>

    <update id="update">
        UPDATE biz_vaccine_record
        <set>
            <if test="petId != null">pet_id = #{petId},</if>
            <if test="vaccineName != null">vaccine_name = #{vaccineName},</if>
            <if test="injectTime != null">inject_time = #{injectTime},</if>
            <if test="nextInjectTime != null">next_inject_time = #{nextInjectTime},</if>
            <if test="remarks != null">remarks = #{remarks},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM biz_vaccine_record WHERE id = #{id}
    </delete>

    <select id="findById" resultType="com.pet.hospital.model.entity.BizVaccineRecord">
        SELECT * FROM biz_vaccine_record WHERE id = #{id}
    </select>
</mapper>
