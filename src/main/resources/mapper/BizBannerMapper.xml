<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pet.hospital.mapper.BizBannerMapper">
    <select id="findActiveBanners" resultType="com.pet.hospital.model.entity.BizBanner">
        SELECT * FROM biz_banner ORDER BY sort_no ASC, id DESC
    </select>

    <select id="findById" resultType="com.pet.hospital.model.entity.BizBanner">
        SELECT * FROM biz_banner WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO biz_banner(image_url, title, link_url, sort_no, is_active, create_time)
        VALUES (#{imageUrl}, #{title}, #{linkUrl}, #{sortNo}, #{isActive}, NOW())
    </insert>

    <update id="update">
        UPDATE biz_banner
        <set>
            <if test="imageUrl != null">image_url = #{imageUrl},</if>
            <if test="title != null">title = #{title},</if>
            <if test="linkUrl != null">link_url = #{linkUrl},</if>
            <if test="sortNo != null">sort_no = #{sortNo},</if>
            <if test="isActive != null">is_active = #{isActive},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM biz_banner WHERE id = #{id}
    </delete>
</mapper>
